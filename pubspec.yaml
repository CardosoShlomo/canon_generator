name: canon_generator
description: "Code generator for canon — emits the typed Screen, Hop, and verb surface from the spec enum."
version: 0.20.0
repository: https://github.com/CardosoShlomo/canon_generator

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  analyzer: ^13.0.0
  build: ^4.0.6
  canon: ^0.16.0
  source_gen: ^4.2.3

dev_dependencies:
  build_test: ^3.5.15
  lints: ^6.0.0
  test: ^1.25.6
